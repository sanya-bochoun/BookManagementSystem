@model IEnumerable<BookManagementSystem.Models.Category>
@{
    ViewData["Title"] = "หมวดหมู่หนังสือ";
}
<h2>@ViewData["Title"]</h2>
<a asp-action="Create" class="btn btn-success mb-3">เพิ่มหมวดหมู่</a>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>ชื่อหมวดหมู่</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>
                <a asp-action="Details" asp-route-id="@item.CategoryId" class="btn btn-info btn-sm">รายละเอียด</a>
                <a asp-action="Edit" asp-route-id="@item.CategoryId" class="btn btn-warning btn-sm">แก้ไข</a>
                <a asp-action="Delete" asp-route-id="@item.CategoryId" class="btn btn-danger btn-sm">ลบ</a>
            </td>
        </tr>
    }
    </tbody>
</table> 